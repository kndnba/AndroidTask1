<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardElevation="@dimen/cardview_indent"
    android:layout_margin="@dimen/cardview_indent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layoutMusic"
        android:layout_width="match_parent"
        android:layout_height="@dimen/recycler_view_item">

        <TextView
            android:id="@+id/musicTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/recycler_view_item"
            android:layout_marginTop="@dimen/textView_marginTop"
            android:text="@string/artist_title"
            android:textColor="@android:color/black"
            android:textSize="@dimen/button_textSize"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>